# Database Script 0x02 - JOIN Queries

This script demonstrates the use of:
- INNER JOIN to retrieve bookings with users
- LEFT JOIN to retrieve properties and reviews
- FULL OUTER JOIN (via UNION) to retrieve all users and bookings
